#include <stdio.h>
int main(){
    int n,i,j;
    scanf("%d", &n);
    for(i=1; i<=n; i++){
        if(i==n) && (i==1){
            printf("*");
        } else{
            

        }
    }
}